import React from "react";import {cleanup,fireEvent,render,screen} from "@testing-library/react";import {afterEach,describe,expect,it} from "vitest";import HealthCalculator from "@/components/calculators/HealthCalculator";
afterEach(cleanup);describe("supporting calculator UX",()=>{it("keeps macro setup user friendly and supports manual and custom modes",()=>{render(<HealthCalculator type="macro-calculator"/>);expect(screen.getByRole("group",{name:"How should calories be set?"})).toBeInTheDocument();const sex=screen.getByLabelText("Sex");expect(sex).toHaveTextContent("Male");expect(sex).toHaveTextContent("Female");expect(sex).not.toHaveTextContent("+5");fireEvent.click(screen.getByRole("radio",{name:"Enter calories"}));expect(screen.getByLabelText("Daily calorie target")).toBeInTheDocument();fireEvent.change(screen.getByLabelText("Macro approach"),{target:{value:"custom"}});expect(screen.getByLabelText("Protein (%)")).toBeInTheDocument();expect(screen.getByLabelText("Carbs (%)")).toBeInTheDocument();expect(screen.getByLabelText("Fat (%)")).toBeInTheDocument()});it("returns a goal-based protein range and meal view",()=>{render(<HealthCalculator type="protein-calculator"/>);fireEvent.change(screen.getByLabelText("Primary goal or activity"),{target:{value:"strength"}});fireEvent.change(screen.getByLabelText("Meals per day"),{target:{value:"4"}});expect(screen.getByLabelText("Calculator results")).toHaveTextContent("105–150 g/day");expect(screen.getByLabelText("Calculator results")).toHaveTextContent("If divided across 4 meals")})});
