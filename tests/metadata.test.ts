import {describe,expect,it} from "vitest";import {siteConfig,publicRoutes,allHumanRoutes} from "@/config/site";
describe("site configuration",()=>{it("uses required exact metadata",()=>{expect(siteConfig.title).toBe("BIBIBOP Nutrition Calculator: Calories, Macros & Allergens");expect(siteConfig.description).toBe("Build your BIBIBOP bowl and calculate calories, protein, carbs, fat, sodium and allergens using official nutrition data for every ingredient.");expect(siteConfig.url).toBe("https://bibibopnutritioncalculator.pro")});it("indexes published calculator routes and the nutrition database but excludes the empty blog",()=>{expect(publicRoutes).not.toContain("/blog/");expect(publicRoutes).toContain("/bibibop-nutrition-facts/");expect(publicRoutes).toContain("/nutrition-calculators/");expect(publicRoutes).toContain("/nutrition-calculators/bmi-calculator/");expect(allHumanRoutes).toContain("/blog/")});it("includes the indexable author profile",()=>{expect(publicRoutes).toContain("/author/m-arslan/")})});
