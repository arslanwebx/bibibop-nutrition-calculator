import {describe,expect,it} from "vitest";import {siteConfig,publicRoutes,allHumanRoutes} from "@/config/site";
describe("site configuration",()=>{it("uses required exact metadata",()=>{expect(siteConfig.title).toBe("BIBIBOP Nutrition Calculator: Calories, Macros & Allergens");expect(siteConfig.description).toBe("Build your BIBIBOP bowl and calculate calories, protein, carbs, fat, sodium and allergens using official nutrition data for every ingredient.");expect(siteConfig.url).toBe("https://bibibopnutritioncalculator.pro")});it("excludes noindex archives from XML routes",()=>{expect(publicRoutes).not.toContain("/blog/");expect(publicRoutes).not.toContain("/nutrition-calculators/");expect(allHumanRoutes).toContain("/blog/")})});
