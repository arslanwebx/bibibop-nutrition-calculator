"use client";
import { useMemo, useState } from "react";
import { categoryLabels, nutritionCategories, nutritionItems, type NutritionCategory } from "@/data/bibibop-nutrition";

export default function NutritionTable(){const [search,setSearch]=useState("");const [category,setCategory]=useState<"all"|NutritionCategory>("all");const [vegan,setVegan]=useState(false);const [sort,setSort]=useState("name");const rows=useMemo(()=>nutritionItems.filter(i=>(category==="all"||i.category===category)&&(!vegan||i.vegan===true)&&`${i.name} ${i.aliases?.join(" ")??""}`.toLowerCase().includes(search.toLowerCase())).sort((a,b)=>sort==="name"?a.name.localeCompare(b.name):(b.nutrients[sort as "calories"].value??-1)-(a.nutrients[sort as "calories"].value??-1)),[search,category,vegan,sort]);const clear=()=>{setSearch("");setCategory("all");setVegan(false);setSort("name")};return <><div className="filters"><label><span className="sr-only">Search items</span><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search item or alias"/></label><label><span className="sr-only">Category</span><select value={category} onChange={e=>setCategory(e.target.value as typeof category)}><option value="all">All categories</option>{nutritionCategories.map(c=><option value={c} key={c}>{categoryLabels[c]}</option>)}</select></label><label><span className="sr-only">Sort</span><select value={sort} onChange={e=>setSort(e.target.value)}><option value="name">Sort by name</option><option value="calories">Calories: high first</option><option value="proteinG">Protein: high first</option><option value="carbsG">Carbs: high first</option><option value="fatG">Fat: high first</option><option value="sodiumMg">Sodium: high first</option></select></label><label className="checkbox"><input type="checkbox" checked={vegan} onChange={e=>setVegan(e.target.checked)}/> Vegan marked</label><button className="button-secondary" type="button" onClick={clear}>Clear filters</button></div><div className="table-wrap"><table className="nutrition-table"><caption>Complete BIBIBOP nutrition facts from the downloadable PDF. Showing {rows.length} of 62 rows.</caption><thead><tr><th scope="col">Item</th><th scope="col">Category</th><th scope="col">Portion</th><th scope="col">Calories</th><th scope="col">Calories from fat</th><th scope="col">Fat (g)</th><th scope="col">Sat. fat (g)</th><th scope="col">Trans fat (g)</th><th scope="col">Chol. (mg)</th><th scope="col">Sodium (mg)</th><th scope="col">Carbs (g)</th><th scope="col">Fiber (g)</th><th scope="col">Sugars (g)</th><th scope="col">Protein (g)</th><th scope="col">Allergens</th><th scope="col">Vegan</th></tr></thead><tbody>{rows.map(i=><tr key={i.id}><td>{i.name}</td><td>{categoryLabels[i.category]}</td><td>{i.portion}</td>{Object.values(i.nutrients).map((n,index)=><td key={index}>{n.display}</td>)}<td>{i.allergenDataStatus!=="listed"?"Not listed":Object.keys(i.allergens).length?Object.keys(i.allergens).join(", "):"None marked"}</td><td>{i.vegan===true?"Yes":i.vegan===false?"No":"Not provided"}</td></tr>)}</tbody></table></div></>}
