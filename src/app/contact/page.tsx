import type {Metadata} from "next";import PageShell from "@/components/content/PageShell";import {ContactForm} from "@/components/forms/Forms";import {siteConfig} from "@/config/site";
export const metadata:Metadata={title:"Contact",description:"Contact the independent BIBIBOP Nutrition Calculator about corrections, access, privacy, or general questions.",alternates:{canonical:"/contact/"}};
export default function Page(){return <PageShell title="Contact" description="Send a correction, accessibility report, privacy request, or general question."><p>You can use the secure form when configured, or email <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>. Messages are reviewed as capacity allows, but a response time is not guaranteed. Please do not include sensitive health, payment, or identity information.</p><ContactForm/></PageShell>}
