import type {Metadata} from "next";import PageShell from "@/components/content/PageShell";import {siteConfig} from "@/config/site";
export const metadata:Metadata={title:"Accessibility",description:"Accessibility features, commitment, and feedback contact for this calculator.",alternates:{canonical:"/accessibility/"}};
export default function Page(){return <PageShell title="Accessibility" description="We aim to make the calculator understandable and operable across devices and input methods."><h2>Supported interactions</h2><p>The site uses semantic headings and landmarks, native form controls, associated labels, keyboard-visible focus, an early skip link, status announcements, table captions and headers, and a mobile menu with an exposed state. Content does not rely on color alone, and reduced-motion preferences are respected.</p><p>Nutrition tables scroll within their container on narrow screens. Calculator controls use practical touch sizes. Form errors and delivery results appear in live status regions.</p><h2>Ongoing improvement</h2><p>Accessibility is an ongoing engineering process, not a certification claim. If a feature prevents access, email <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a> with the page, task, browser or assistive technology if you wish to share it, and a description of the problem. We will review actionable reports and prioritize fixes based on impact.</p></PageShell>}
