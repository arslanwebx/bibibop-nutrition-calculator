import type {Metadata} from "next";import PageShell from "@/components/content/PageShell";import {siteConfig} from "@/config/site";
export const metadata:Metadata={title:"Corrections Policy",description:"How to report and how we verify corrections to nutrition data and site content.",alternates:{canonical:"/corrections-policy/"}};
export default function Page(){return <PageShell title="Corrections policy" description="Specific, source-linked correction reports help us investigate efficiently."><h2>Report a possible error</h2><p>Email <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a> or use the <a href="/contact/">contact form</a>. Include the affected page, ingredient or statement, the value you believe is wrong, a primary-source link or file, and the date you observed it. Do not send sensitive medical information.</p><h2>Verification</h2><p>We compare the report with the current official sheet and its version date. A webpage discrepancy does not automatically replace a PDF value because the calculator uses one complete source baseline. If a new sheet is available, all rows are checked to avoid mixing versions.</p><h2>Publishing corrections</h2><p>Confirmed small typographical fixes are corrected promptly. Material nutrition or methodology changes receive a dated version-history entry. If a claim cannot be verified, we may clarify the uncertainty rather than replace one unverified value with another.</p></PageShell>}
