import type { Metadata } from "next";import PageShell from "@/components/content/PageShell";import {siteConfig} from "@/config/site";
export const metadata:Metadata={title:"About",description:"About this independent BIBIBOP nutrition data calculator and its source-first review process.",alternates:{canonical:"/about/"}};
export default function Page(){return <PageShell title="About this calculator" description="A focused, independent tool for totaling publicly available BIBIBOP nutrition values."><section className="policy-section"><h2>Why this site exists</h2><p>{siteConfig.name} helps people combine the individual values in BIBIBOP's published nutrition sheet. It turns a long table into a usable bowl calculator while preserving portions, less-than values, unavailable fields, allergen gaps, and source dates.</p><p>This site is not affiliated with or endorsed by BIBIBOP Asian Grill. It does not reproduce the restaurant's branding, make ordering recommendations, or provide medical advice.</p><h2>How information is handled</h2><p>The complete downloadable PDF is the calculator baseline. Automated checks enforce the expected 62 rows and required nutrient fields. The live restaurant webpage is reviewed separately because it contains conflicting values. Updates are dated, and reported corrections are checked against a primary source before publication.</p><h2>Contact</h2><p>Questions, source corrections, privacy requests, and accessibility feedback are welcome at <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a> or through the <a href="/contact/">contact form</a>.</p></section></PageShell>}
